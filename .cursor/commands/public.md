# /public — 同步到发布目录（不 push）

**指令**：脱敏 + 开源规范检查 + 同步到 public；不执行 commit/push。当未在 /merge 中同步时使用。

**详见**：[spec/CONVENTIONS.md](../spec/CONVENTIONS.md) **第一节**（/public）、**第八节 8.1**（表格 /public）。有歧义以 CONVENTIONS 为准。
