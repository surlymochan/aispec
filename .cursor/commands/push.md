# /push — 推送发布目录

**指令**：在 public 完成后，于发布目录内完成 commit（若有未提交变更）与 push，由 Agent 自动执行。提交身份与 amend 等操作按 **CONVENTIONS** 及业务项目约定执行。

**详见**：[spec/CONVENTIONS.md](../spec/CONVENTIONS.md) **第一节**（工作流 /push）；提交身份、amend 与 Co-authored-by 规避见**业务项目 aispec.md 2.4**。有歧义以 CONVENTIONS 为准。
