# SP0001 MRD：初始变更收敛（通用部分）

本迭代为 **aispec 项目首次落盘**，不引入新约定。

**需求要点**：

- 将自 openclaw-cn-private/SP0219 迁出的**工作流、项目结构、部署与文档**方面的通用变更，以 SP 形式落盘于本仓库。
- 产出：迭代目录内 MRD/README/CHANGELOG，合并后形成 spec 的当前发布版本（版本号与 CONVENTIONS）。

由负责人触发（/mrd），Agent 产出本迭代文档并落盘于本目录；验收后合并到 spec/。
